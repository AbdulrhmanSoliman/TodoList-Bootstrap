{
  "name": "todolist",
  "version": "1.0.0",
  "description": "This is a simple and elegant Todo List application built with HTML, CSS, and JavaScript. The app uses Bootstrap for styling and FontAwesome for icons. It allows users to add, edit, delete, and mark tasks as complete. The app also supports dark mode and saves tasks in the browser's local storage.",
  "main": "index.js",
  "scripts": {
    "start": "vite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.83.0",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3"
  }
}
